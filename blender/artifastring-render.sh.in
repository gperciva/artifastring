#!/bin/sh
TEMP_DIR=/tmp/vivi-movie/
mkdir -p $TEMP_DIR
rm -f $TEMP_DIR/*.tga

blender -noaudio \
  -b @datarootdir@/artifastring/fast-violin.blend \
  -P @datarootdir@/artifastring/artifastring-blender.py \
  -o $TEMP_DIR/#### \
  -s $2 -e $3 -a \
  -- \
  -f $1 \
  > artifastring-render.log

